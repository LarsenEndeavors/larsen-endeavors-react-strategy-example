export const StringIsAlpha = (props) => {
    return <p>{props.input} contains only alpha characters</p>
}

export const StringIsNumeric = (props) => {
    return <p>{props.input} contains only numeric characters</p>
}

export const StringIsMixed = (props) => {
    return <p>{props.input} contains mixed characters</p>
}